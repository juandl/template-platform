//Type
import type { DefineRoutesFunction } from "@remix-run/dev/dist/config/routes";

/**
 * @description Routers app
 */
export const AppRouters = (defineRoutes: DefineRoutesFunction) => {
	return defineRoutes((route) => {});
};
